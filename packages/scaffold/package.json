{
  "name": "@repo/scaffold",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./browser": "./src/browser.ts",
    "./recipe-preview": "./src/RecipePreviewSchema.ts"
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "scratch": "bun run ./scratchpad.ts",
    "test": "vitest run",
    "clean": "git clean -xdf .cache .turbo dist node_modules tsconfig.tsbuildinfo"
  },
  "dependencies": {
    "@effect-vfs/memory": "^0.5.0",
    "@repo/domain": "workspace:*",
    "@repo/catalog": "workspace:*",
    "effect": "4.0.0-rc.115",
    "effect-boxes": "^0.17.2",
    "ts-morph": "^28.0.0"
  },
  "devDependencies": {
    "@effect/vitest": "4.0.0-rc.115",
    "@types/node": "^26.6.2",
    "@repo/config-typescript": "workspace:*",
    "vitest": "^5.0.1"
  }
}

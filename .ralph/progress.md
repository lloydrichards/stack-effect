# Ralph Progress

Use short dated entries. Keep them factual.

- No iterations have run yet.
- 2026-04-21: Defined the first real Ralph task as fixing the root feedback loop. Excluded `e2e/` and `apps/client/` from Bun test discovery and replaced a flaky dynamic-import server smoke test with direct domain imports. Files changed: `bunfig.toml`, `apps/server/src/index.test.ts`, `.ralph/prd.json`, `.ralph/progress.md`.
- 2026-04-21: Verified the root feedback loop passes with `bun lint`, `bun run type-check`, and `bun test`. Marked the infrastructure slice complete in `.ralph/prd.json`.
- 2026-04-21: Completed issue #4 by adding a new `@repo/selection` package with a public `parseSelection` validation surface for explicit targets and repo modules, including repo-only requests and rejection of empty or malformed selections. Chose strict trimmed non-empty string validation and kept the cross-field non-empty rule in `parseSelection`. Files changed: `packages/selection/package.json`, `packages/selection/tsconfig.json`, `packages/selection/src/index.ts`, `packages/selection/src/index.test.ts`, `bun.lock`, `.ralph/prd.json`, `.ralph/progress.md`.
- 2026-04-21: Completed issue #5 by constraining selection targets to v1 `app/<name>` and `package/<name>` identities, exposing `getTargetIdentity`, and rejecting duplicate conceptual targets during `parseSelection`. Kept collision checks order-independent by keying on normalized kind/name identity and verified the full repo feedback loop passes. Files changed: `packages/selection/src/index.ts`, `packages/selection/src/index.test.ts`, `.ralph/prd.json`, `.ralph/progress.md`.
- 2026-04-21: Completed issue #6 by extending `Selection` with explicit `targetModules`, normalizing duplicate repo and target-module selections into a stable order, and rejecting unsupported module attachments outside canonical `package/domain -> domain-api`. Verified with `bun lint`, `bun run type-check`, and `bun test`. Files changed: `packages/selection/src/index.ts`, `packages/selection/src/index.test.ts`, `.ralph/prd.json`, `.ralph/progress.md`.
- 2026-04-21: Completed issue #7 by adding a public `resolveBlueprint` surface that turns simple selections into deterministic resolved targets and repo modules with selected status and structured selection causes. Deferred dependency expansion by returning a typed failure for non-empty `targetModules` until the next blueprint slices land. Verified with `bun lint`, `bun run type-check`, and `bun test`. Files changed: `packages/selection/src/index.ts`, `packages/selection/src/index.test.ts`, `.ralph/prd.json`, `.ralph/progress.md`.
- 2026-04-21: Completed issue #8 by teaching `resolveBlueprint` to imply `root-bootstrap` for every selected target while preserving explicit selection precedence and dependency provenance on the resolved repo module. Kept target-module resolution deferred for later blueprint slices. Verified with `bun lint`, `bun run type-check`, and `bun test`. Files changed: `packages/selection/src/index.ts`, `packages/selection/src/index.test.ts`, `.ralph/prd.json`, `.ralph/progress.md`.
- 2026-04-21: Completed issue #9 by resolving attached `domain-api` modules into owned blueprint target-module nodes with canonical `package/domain` identity and by moving compatibility failures into typed blueprint resolution errors. Kept parse-time validation strict while allowing blueprint tests to exercise invalid target-module inputs directly. Verified with `bun lint`, `bun run type-check`, and `bun test`. Files changed: `packages/selection/src/index.ts`, `packages/selection/src/index.test.ts`, `.ralph/prd.json`, `.ralph/progress.md`.
- 2026-04-21: Completed issue #10 by teaching `resolveBlueprint` to imply canonical `package/domain` when `domain-api` is selected without its owning target and to preserve explicit target selection precedence when both paths converge. Kept the change local to blueprint normalization and covered both implied and explicit-overlap scenarios in tests. Files changed: `packages/selection/src/index.ts`, `packages/selection/src/index.test.ts`, `.ralph/prd.json`, `.ralph/progress.md`.
- 2026-04-21: Completed issue #11 by teaching `resolveBlueprint` that canonical `app/server` implies the `package/domain` `domain-api` contract, merging the implied module and target with explicit `package/domain` selection when both paths meet. Verified with `bun lint`, `bun run type-check`, and `bun test`. Files changed: `packages/selection/src/index.ts`, `packages/selection/src/index.test.ts`, `.ralph/prd.json`, `.ralph/progress.md`.
- 2026-04-21: Completed issue #12 by adding typed blueprint warnings for implied dependencies, redundant selections, and duplicate normalization while keeping resolved output deterministic and duplicate-free. Chose to report duplicate normalization in `resolveBlueprint` so direct resolver callers still get structured warnings even when `parseSelection` has already canonicalized input. Verified with `bun lint`, `bun run type-check`, and `bun test`. Files changed: `packages/selection/src/index.ts`, `packages/selection/src/index.test.ts`, `.ralph/prd.json`, `.ralph/progress.md`.
- 2026-04-21: Completed issue #13 by adding a narrow blueprint-time `targetCompositions` surface for package targets and resolving canonical `package/domain` to `./Api` when `domain-api` is attached, while keeping override mechanics internal to blueprint normalization. Verified with `bun lint`, `bun run type-check`, and `bun test`. Files changed: `packages/selection/src/index.ts`, `packages/selection/src/index.test.ts`, `.ralph/prd.json`, `.ralph/progress.md`.
- 2026-04-21: Completed issue #14 by adding a flat deterministic `blueprint.intents` list derived after dependency expansion and composition resolution, while keeping the reviewed resolved graph intact. Covered the first reviewed server slice and package entrypoint outputs in tests, then verified with `bun lint`, `bun run type-check`, and `bun test`. Files changed: `packages/selection/src/index.ts`, `packages/selection/src/index.test.ts`, `.ralph/prd.json`, `.ralph/progress.md`.
- 2026-04-21: Reconciled Ralph tracking with the completed blueprint work by marking issue #14 passed in `.ralph/prd.json` after verifying the implemented scaffold flow now lives in the shared domain and scaffold packages. Re-verified with `bun lint`, `bun run type-check`, and `bun test`. Files changed: `.ralph/prd.json`, `.ralph/progress.md`.
- 2026-04-21: Completed issue #19 by restoring a minimal `@repo/scaffold` workspace and defining the public schema-backed `Plan` contract for classified entries, tree nodes, merge requirements, warnings, and structured causes. Chose a narrow planning model with explicit `needsMergeStrategy` support and added package tests that exercise the first-slice `package/domain` shape. Verified with `bun install`, `bun lint`, `bun run type-check`, and `bun test`. Files changed: `packages/scaffold/package.json`, `packages/scaffold/tsconfig.json`, `packages/scaffold/src/index.ts`, `packages/scaffold/src/index.test.ts`, `bun.lock`, `.ralph/prd.json`, `.ralph/progress.md`.
- 2026-04-21: Completed issue #21 by adding a narrow `RepoSnapshotLoader.load` surface in `@repo/scaffold` that derives selective snapshot paths from the resolved blueprint, records root entries, and loads file contents or directory existence without modeling the whole repository. Kept `PlanService.build` limited to the empty-root bootstrap slice while routing repo inspection through the new loader so later classification work can reuse the snapshot boundary. Verified with `bun lint`, `bun run type-check`, and `bun test`. Files changed: `packages/scaffold/src/index.ts`, `packages/scaffold/src/index.test.ts`, `.ralph/prd.json`, `.ralph/progress.md`.
- 2026-04-21: Completed issue #24 by projecting the first-slice `app/server` base paths directly from the current blueprint target graph, including `http-api-server` module provenance on server source paths without re-running blueprint resolution inside planning. Kept classification behavior unchanged for this slice and verified flat entry plus tree ordering coverage before re-running `bun lint`, `bun run type-check`, and `bun test`. Files changed: `packages/scaffold/src/index.ts`, `packages/scaffold/src/index.test.ts`, `.ralph/prd.json`, `.ralph/progress.md`.
- 2026-04-21: Completed issue #25 by classifying root-bootstrap authoritative files against the selective repo snapshot so existing repositories can produce `create`, `modify`, and `unchanged` plan entries without failing on unrelated root files. Kept non-file collisions as typed planning errors and covered existing-repo classification plus directory collision behavior before re-running `bun lint`, `bun run type-check`, and `bun test`. Files changed: `packages/scaffold/src/index.ts`, `packages/scaffold/src/index.test.ts`, `.ralph/prd.json`, `.ralph/progress.md`.
- 2026-04-21: Completed issue #23 by projecting canonical implied `package/domain` planning paths directly from the confirmed blueprint target graph, preserving target-level provenance on `package.json` and module-level provenance on `src/Api.ts`. Kept the slice to deterministic path derivation only and left package export merge handling for the next planning issue. Files changed: `packages/scaffold/src/index.ts`, `packages/scaffold/src/index.test.ts`, `.ralph/prd.json`, `.ralph/progress.md`.
- 2026-04-21: Completed issue #22 by reading `package/domain` target composition during planning so the first-slice `public-entrypoint` contract now contributes `./Api` provenance to `packages/domain/package.json` without introducing package export merge behavior yet. Verified with `bun lint`, `bun run type-check`, and `bun test`. Files changed: `packages/scaffold/src/index.ts`, `packages/scaffold/src/index.test.ts`, `.ralph/prd.json`, `.ralph/progress.md`.
- 2026-04-22: Completed issue #26 by teaching `@repo/scaffold` to recognize flat string `package.json` export maps for the first-slice `package/domain` flow, classifying additive `./Api` exports as `modify`, matching exports as `unchanged`, and unsupported export shapes as `needsMergeStrategy` with structured warnings. Kept the merge logic narrow to known string export maps and verified with `bun lint`, `bun run type-check`, and `bun test`. Files changed: `packages/scaffold/src/index.ts`, `packages/scaffold/src/index.test.ts`, `.ralph/prd.json`, `.ralph/progress.md`.
- 2026-04-22: Completed issue #27 by extending the narrow `packages/domain/package.json` planner path to recognize additive `dependencies` and `devDependencies`, preserve section attribution in merge requirements, and defer unsupported section shapes to `needsMergeStrategy`. Kept the behavior scoped to the first-slice package flow and verified with `bun lint`, `bun run type-check`, and `bun test`. Files changed: `packages/scaffold/src/index.ts`, `packages/scaffold/src/index.test.ts`, `.ralph/prd.json`, `.ralph/progress.md`.
- 2026-04-22: Completed issue #28 by teaching `@repo/scaffold` to recognize simple `packages/domain/src/index.ts` barrel files, classify additive `export * from "./Api";` merges as `modify`, preserve exact matches as `unchanged`, and defer unsupported barrel shapes to `needsMergeStrategy`. Kept the logic narrow to simple star-export barrels for the first-slice `public-entrypoint` flow and verified with `bun lint`, `bun run type-check`, and `bun test`. Files changed: `packages/scaffold/src/index.ts`, `packages/scaffold/src/index.test.ts`, `.ralph/prd.json`, `.ralph/progress.md`.
- 2026-04-22: Completed issue #29 by teaching `@repo/scaffold` to project the known first-slice `apps/server/tsconfig.json` and `packages/domain/tsconfig.json` files and classify them conservatively as `create`, exact `unchanged`, or `needsMergeStrategy`. Chose exact-content matching only and deferred all tsconfig merge behavior to structured merge requirements with path-level attribution. Files changed: `packages/scaffold/src/index.ts`, `packages/scaffold/src/index.test.ts`, `.ralph/prd.json`, `.ralph/progress.md`.
- 2026-04-22: Completed issue #30 by teaching `@repo/scaffold` to recognize additive `scripts` merges for the current `apps/server/package.json` and `packages/domain/package.json` planner paths, classify matching scripts as `unchanged`, and defer unsupported script shapes to `needsMergeStrategy` with path-level script attribution. Kept the merge logic narrow to flat string `scripts` records alongside the existing package export and dependency handling, then verified with `bun lint`, `bun run type-check`, and `bun test`. Files changed: `packages/scaffold/src/index.ts`, `packages/scaffold/src/index.test.ts`, `.ralph/prd.json`, `.ralph/progress.md`.
- 2026-04-22: Completed issue #32 by locking in the existing `needsMergeStrategy` behavior with a regression that mixes an unsupported `packages/domain/package.json` export shape with unaffected planned files, proving planning continues and preserves path-level merge attribution. The implementation already satisfied the issue, so this slice only added focused coverage before re-verifying with `bun lint`, `bun run type-check`, and `bun test`. Files changed: `packages/scaffold/src/index.test.ts`, `.ralph/prd.json`, `.ralph/progress.md`.
- 2026-04-22: Completed issue #31 by expanding the real-temp-dir `@repo/scaffold` planning coverage to a mixed end-to-end fixture that asserts deterministic flat entry ordering, top-level tree ordering, and structured provenance plus merge-requirement continuation through the public `Plan` output. Kept the slice test-only because the planner already satisfied the behavior; verified with `bun lint`, `bun run type-check`, and `bun test`. Files changed: `packages/scaffold/src/index.test.ts`, `.ralph/prd.json`, `.ralph/progress.md`.
- 2026-04-22: Closed the final repo feedback-loop gap with an out-of-scope cleanup by removing an unused `effect/TxHashMap` import from `packages/scaffold/src/plan_prototype.ts`. `.ralph/prd.json` remained empty, and the repo now passes `bun lint`, `bun run type-check`, and `bun test`. Files changed: `packages/scaffold/src/plan_prototype.ts`, `.ralph/progress.md`.
- 2026-04-22: Completed issue #34 by promoting the public planning contract into `@repo/domain/Plan`, exporting `PlanService` from `@repo/scaffold`, and adding an end-to-end package entrypoint test that builds and decodes a typed plan from a resolved blueprint. Kept planner implementation in `packages/scaffold/src/plan_prototype.ts` for now and reused the new domain schema there instead of introducing a second contract. Files changed: `packages/domain/src/Plan.ts`, `packages/domain/package.json`, `packages/scaffold/src/index.ts`, `packages/scaffold/src/index.test.ts`, `packages/scaffold/src/plan_prototype.ts`, `.ralph/prd.json`, `.ralph/progress.md`.
- 2026-04-22: Completed issue #35 by locking in selective repo snapshot loading with a production `RepoSnapshotLoader` regression that proves deterministic ordering, root entry capture, and path scoping for the current planning blueprint. Kept the implementation unchanged because the planner already requested only projected file and directory paths needed for classification and tree projection. Files changed: `packages/scaffold/src/plan_prototype.test.ts`, `.ralph/prd.json`, `.ralph/progress.md`.
- 2026-04-22: Completed issue #36 by teaching the production planner to synthesize the authoritative root-bootstrap file set for repo-only bootstrap blueprints and by adding a public `PlanService` regression for empty-repo planning through `@repo/scaffold`. Kept the bootstrap projection narrow to repo-only root-bootstrap selections so existing target-driven planning behavior stays unchanged. Files changed: `packages/scaffold/src/index.test.ts`, `packages/scaffold/src/plan_prototype.ts`, `.ralph/prd.json`, `.ralph/progress.md`.
- 2026-04-22: Completed issue #37 by promoting non-empty repo root-bootstrap reconciliation coverage to the public `@repo/scaffold` entrypoint, proving bootstrap-owned files classify as `create`, `modify`, or `unchanged` while unrelated root files do not block planning. Kept the slice test-only because the production planner already satisfied the behavior verified in lower-level coverage. Files changed: `packages/scaffold/src/index.test.ts`, `.ralph/prd.json`, `.ralph/progress.md`.
- 2026-04-22: Completed issue #38 by promoting base `app/server` target projection coverage to the public `@repo/scaffold` entrypoint, resolving a plain server selection through `BlueprintService` and proving deterministic base-path ordering plus target-only provenance without `http-api-server` module output. Kept the slice test-only because the production planner already projected the expected base server paths. Files changed: `packages/scaffold/src/index.test.ts`, `.ralph/prd.json`, `.ralph/progress.md`.
- 2026-04-22: Completed issue #39 by promoting `http-api-server` module projection coverage to the public `@repo/scaffold` entrypoint, resolving a server selection with the module through `BlueprintService` and proving the module-owned API files plus merged target/module provenance in both flat entries and the tree. Kept the slice test-only because the production planner already projected the expected server module paths. Files changed: `packages/scaffold/src/index.test.ts`, `.ralph/prd.json`, `.ralph/progress.md`.
- 2026-04-22: Completed issue #40 by promoting canonical `package/domain` projection coverage to the public `@repo/scaffold` entrypoint, resolving the first-slice server selection with `http-api-server` through `BlueprintService` and proving the implied package target plus `domain-api` module appear in the plan entries and tree without re-resolving blueprint logic inside planning. Kept the slice test-only because the production planner already projected the expected package paths. Files changed: `packages/scaffold/src/index.test.ts`, `.ralph/prd.json`, `.ralph/progress.md`.
- 2026-04-22: Completed issue #41 by promoting package-composition projection coverage to the public `@repo/scaffold` entrypoint, proving the implied `./Api` composition adds `packages/domain/src/index.ts` alongside the existing package paths and preserves the expected composition-driven causes through `PlanService`. Kept the slice test-only because the production planner already projected the first-slice public entrypoint behavior. Files changed: `packages/scaffold/src/index.test.ts`, `.ralph/prd.json`, `.ralph/progress.md`.
- 2026-04-22: Completed issue #42 by promoting barrel export merge coverage to the public `@repo/scaffold` entrypoint, proving `PlanService` classifies additive, matching, and unsupported `packages/domain/src/index.ts` barrel shapes through resolved blueprint inputs. Kept the slice test-only after confirming the production contract attributes the planned barrel file to the implied `domain-api` module rather than the composition cause. Files changed: `packages/scaffold/src/index.test.ts`, `.ralph/prd.json`, `.ralph/progress.md`.
- 2026-04-22: Completed issue #43 by promoting package export merge coverage to the public `@repo/scaffold` entrypoint, proving `PlanService` classifies additive, matching, and unsupported `packages/domain/package.json` export shapes through resolved blueprint inputs. Kept the slice test-only after confirming the public contract attributes the package file to the implied package target/module causes while export merge requirements stay path-local to the implied `domain-api` module. Files changed: `packages/scaffold/src/index.test.ts`, `.ralph/prd.json`, `.ralph/progress.md`.
- 2026-04-22: Completed issue #44 by promoting package dependency merge coverage to the public `@repo/scaffold` entrypoint, proving `PlanService` classifies create, additive modify, matching unchanged, and incompatible dependency-section cases through resolved blueprint inputs. Kept the slice test-only because the production planner already handled the behavior, and verified dependency merge requirements preserve `dependencies` vs `devDependencies` attribution in the public contract. Files changed: `packages/scaffold/src/index.test.ts`, `.ralph/prd.json`, `.ralph/progress.md`.
- 2026-04-22: Completed issue #45 by promoting package script merge coverage to the public `@repo/scaffold` entrypoint, proving `PlanService` classifies additive server script merges, matching package scripts, and unsupported script shapes with path-level script attribution. Kept the slice test-only because the production planner already handled the behavior, and re-verified with `bun lint`, `bun run type-check`, and `bun test`. Files changed: `packages/scaffold/src/index.test.ts`, `.ralph/prd.json`, `.ralph/progress.md`.
- 2026-04-22: Completed issue #46 by promoting conservative tsconfig planning coverage to the public `@repo/scaffold` entrypoint, proving scaffold tsconfig files classify as `create` when absent, `unchanged` on exact content matches, and `needsMergeStrategy` when existing contents differ. Kept the slice test-only because the production planner already handled tsconfig classification conservatively. Files changed: `packages/scaffold/src/index.test.ts`, `.ralph/prd.json`, `.ralph/progress.md`.
- 2026-04-22: Completed issue #47 by promoting mixed ambiguous-path coverage to the public `@repo/scaffold` entrypoint, proving a `packages/domain/package.json` export merge requirement does not stop unaffected server and domain paths from classifying and projecting through `PlanService`. Kept the slice test-only because the production planner already accumulated path-local merge requirements and warnings correctly. Files changed: `packages/scaffold/src/index.test.ts`, `.ralph/prd.json`, `.ralph/progress.md`.
- 2026-04-22: Advanced issue #48 with a narrow migration slice by promoting the prototype mixed end-to-end ordering/provenance regression into the public `@repo/scaffold` entrypoint. Chose to keep scope test-only and leave broader prototype-test retirement for later slices. Files changed: `packages/scaffold/src/index.test.ts`, `.ralph/prd.json`, `.ralph/progress.md`.
- 2026-04-22: Advanced issue #48 with a narrow retirement slice by deleting the now-duplicated prototype mixed end-to-end ordering/provenance regression from `packages/scaffold/src/plan_prototype.test.ts` after keeping the migrated public `PlanService` coverage in `packages/scaffold/src/index.test.ts` as the source of truth. Kept scope test-only and left broader prototype-test retirement for later slices. Files changed: `packages/scaffold/src/plan_prototype.test.ts`, `.ralph/prd.json`, `.ralph/progress.md`.
- 2026-04-22: Advanced issue #48 with a narrow collision-handling slice by promoting the prototype root-bootstrap directory-collision rejection regression into the public `@repo/scaffold` entrypoint and deleting the duplicate prototype test. Kept scope test-only and left the remaining prototype-path retirement for later slices. Files changed: `packages/scaffold/src/index.test.ts`, `packages/scaffold/src/plan_prototype.test.ts`, `.ralph/prd.json`, `.ralph/progress.md`.
- 2026-04-22: Advanced issue #48 with a narrow tsconfig retirement slice by deleting duplicated prototype tsconfig regressions after confirming matching and incompatible scaffold tsconfig coverage already lives in the public `@repo/scaffold` entrypoint tests. Kept scope test-only and left the remaining prototype-path retirement for later slices. Files changed: `packages/scaffold/src/plan_prototype.test.ts`, `.ralph/prd.json`, `.ralph/progress.md`.
- 2026-04-22: Advanced issue #48 with a narrow package-export retirement slice by deleting duplicated prototype package export merge regressions after confirming additive, matching, and unsupported export coverage already lives in the public `@repo/scaffold` entrypoint tests. Kept scope test-only and left the remaining prototype-path retirement for later slices. Files changed: `packages/scaffold/src/plan_prototype.test.ts`, `.ralph/prd.json`, `.ralph/progress.md`.
- 2026-04-22: Advanced issue #48 with a narrow package-dependency retirement slice by deleting duplicated prototype matching and incompatible-dependency regressions after confirming equivalent public `@repo/scaffold` entrypoint coverage remains the source of truth. Kept the unsupported dependency-shape prototype regression because public coverage does not yet replace that exact case. Files changed: `packages/scaffold/src/plan_prototype.test.ts`, `.ralph/prd.json`, `.ralph/progress.md`.
- 2026-04-22: Advanced issue #48 with a narrow unsupported-dependency retirement slice by promoting the prototype array-shaped `packages/domain/package.json` dependency regression into the public `@repo/scaffold` entrypoint and deleting the duplicate prototype test. Kept scope test-only and left the remaining prototype-path retirement for later slices. Files changed: `packages/scaffold/src/index.test.ts`, `packages/scaffold/src/plan_prototype.test.ts`, `.ralph/prd.json`, `.ralph/progress.md`.
- 2026-04-22: Advanced issue #48 with a narrow package-script retirement slice by deleting duplicated prototype package-script regressions after confirming equivalent public `@repo/scaffold` entrypoint coverage remains the source of truth for additive server scripts, matching package scripts, and unsupported script shapes. Kept scope test-only and left the remaining prototype-path retirement for later slices. Files changed: `packages/scaffold/src/plan_prototype.test.ts`, `.ralph/prd.json`, `.ralph/progress.md`.
- 2026-04-22: Advanced issue #48 with a narrow plan-contract retirement slice by moving the remaining shared plan schema decode assertions into `packages/domain/src/Plan.test.ts` and deleting the duplicate prototype contract tests from `packages/scaffold/src/plan_prototype.test.ts`. Kept scope test-only and left the remaining prototype-path retirement for later slices. Files changed: `packages/domain/src/Plan.test.ts`, `packages/scaffold/src/plan_prototype.test.ts`, `.ralph/prd.json`, `.ralph/progress.md`.
- 2026-04-22: Advanced issue #48 with a narrow root-bootstrap retirement slice by deleting duplicated prototype empty-repo and non-empty-repo bootstrap regressions after confirming equivalent public `PlanService` coverage remains in `packages/scaffold/src/index.test.ts`. Kept scope test-only and left the remaining prototype-path retirement for later slices. Files changed: `packages/scaffold/src/plan_prototype.test.ts`, `.ralph/prd.json`, `.ralph/progress.md`.
- 2026-04-22: Completed issue #48 by deleting the remaining `packages/scaffold/src/plan_prototype.test.ts` file after confirming planning behavior is now covered through the public `PlanService` and domain contract tests, while the leftover selective snapshot checks only exercised an internal loader implementation detail. Kept scope test-only and retired the last prototype-path dependency from the scaffold suite. Files changed: `packages/scaffold/src/plan_prototype.test.ts`, `.ralph/prd.json`, `.ralph/progress.md`.

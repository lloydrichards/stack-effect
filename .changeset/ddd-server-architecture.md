---
"stack-effect": minor
---

Add a default-off, architecture-scoped DDD Todo catalog to the CLI and Recipe Builder. It generates the canonical `server/api` HTTP API and React Effect Atom client with a Memory baseline, additive SQLite and PostgreSQL providers, Node and Bun SQLite support, and atomic generation while preserving Classic behavior exactly.

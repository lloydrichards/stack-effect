---
"stack-effect": patch
---

Keep optional CLI switches disabled when omitted, generate a Vite Plus toolchain compatible with the Effect compiler plugin, and keep terminal chat compatible with the generated Effect dependency baseline.
